import PropTypes from 'prop-types';

const blog = (blog) => {
    return (
        <div>
            
        </div>
    );
};
Blog.propsType = {
    Blog:PropTypes.object.isRequired
}
export default blog;